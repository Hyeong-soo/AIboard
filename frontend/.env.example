# Base URL for API requests (frontend -> backend)
# For local Vite dev server (backend on host):
# VITE_API_BASE_URL=http://localhost:3000
# For Docker build on macOS/Windows (backend on host machine):
# VITE_API_BASE_URL=http://host.docker.internal:3000
VITE_API_BASE_URL=
